import asyncDialog from './async-dialog';
import dialog from './dialog';
import type { AsyncDialogRef, DialogRef, DialogProps } from './types';

export { asyncDialog, dialog };
export type { AsyncDialogRef, DialogRef, DialogProps };
